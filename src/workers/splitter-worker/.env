# Kafka Configuration
KAFKA_BROKERS=localhost:29092
KAFKA_CLIENT_ID=splitter-worker
KAFKA_GROUP_ID=splitter-worker-group
KAFKA_RETRIES=8
KAFKA_INITIAL_RETRY_TIME=100

# Cache Configuration (for message deduplication)
CACHE_MAX_SIZE=10000
